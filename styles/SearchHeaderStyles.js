import styled from 'styled-components';

const SearchHeaderStyles = styled.div`
    display: flex;
    align-items: center;
    justify-content: space-between;
    .total {
        font-size: 1.7rem;
        color: var(--grey);
        margin-bottom: 3rem;
    }
`;

export default SearchHeaderStyles;
